export enum PathLink {
  words = '/words',
  wordId = '/words/',
  user = '/users',
  tokens = '/tokens',
  aggregatedWords = '/aggregatedWords',
  statistics = '/statistics',
  settings = '/settings',
  signIn = '/signin',
}

export enum ErrorsCode {
  code500 = 500,
  code422 = 422,
  code200 = 200,
}
